# Python runtime artifacts
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# PyInstaller build artifacts (feature 008 single-exe build)
build/
dist/
*.manifest

# Release packaging output (binary ships as a release asset, never committed)
*.zip

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store

# Dev tooling artifacts (kept out of the public repo)
.claude/
.specify/
specs/
.serena/
CLAUDE.md

# Machine-specific shortcuts
*.lnk
