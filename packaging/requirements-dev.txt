# Dev / build dependencies for the single-exe + GUI build (feature 008).
# Install on the BUILD machine only; the produced exe bundles what it needs.
#
#   python -m pip install -r packaging/requirements-dev.txt
#
# Requires Python 3.11+ (plan.md "Technical Context").

# ── GUI host + freezing toolchain ─────────────────────────────────
PySide6>=6.6            # Qt 6: tabs, QSystemTrayIcon, QWebEngineView, audio
PyInstaller>=6.3        # one-folder freeze + Splash target (feature 010)
pywin32>=306            # win32crypt (DPAPI), CREATE_NO_WINDOW, Job Objects

# ── carried server runtime deps (bundled, run unchanged — Principle V) ──
numpy
scipy
sounddevice
websockets
